version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm config set @rocketclimb:registry https://npm.pkg.github.com
        - npm config set //npm.pkg.github.com/:_authToken TEST #$GITHUB_TOKEN # temp comment
        - npm install --cache .npm --prefer-offline
    build:
      commands:
        - npm run build-all
  artifacts:
    baseDirectory: packages/ignition/.next
    files:
      - "**/*"
  cache:
    paths:
      - packages/ignition/.next/cache/**/*
      - .npm/**/*
