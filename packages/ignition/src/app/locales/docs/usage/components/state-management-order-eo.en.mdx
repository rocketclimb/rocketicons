---
title: Using the `odd` and `even` modifiers
description: Using the `odd` and `even` modifiers
slug: state-management-order-eo
group: state-management
order: 5
activeSelector: group-has-[.docs-state-management]:active-content
---

You can also style an icon when it’s an odd or even child using the `odd` and `even` modifiers:
