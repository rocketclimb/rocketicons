import definitions from "./colors.json";

export const forAnimation = definitions.animated;

export const colors = definitions.colors;

export const variations = definitions.variations;

export const colorsUtilities = definitions.utilities;

export const colorsTable = definitions.table;
